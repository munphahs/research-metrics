import React from "react";
import { createRoot } from "react-dom/client";
import MUNDashboard from "./MUNDashboard.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MUNDashboard />
  </React.StrictMode>
);
